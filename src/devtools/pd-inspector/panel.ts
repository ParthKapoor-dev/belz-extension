// "PD Inspector" DevTools panel entry. The panel itself is PdInspectorPanel
// (inspector-panel.ts); this file only starts it.
import { PdInspectorPanel } from './inspector-panel';

new PdInspectorPanel().start();
